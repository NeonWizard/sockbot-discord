{
  "scripts": {
    "start": "ts-node src/Bot.ts",
    "build": "yarn tsc",
    "docker:build": "docker build -t averagewizard13/sockbot-discord:dev .",
    "docker:push": "docker push averagewizard13/sockbot-discord:dev",
    "docker:startdb": "docker compose up",
    "docker:stopdb": "docker compose down"
  },
  "dependencies": {
    "discord.js": "^14.1.2",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.6.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
